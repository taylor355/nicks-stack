# Default model: Anthropic claude-sonnet-5 — the "smart" mode of the provider
# routing layer (/root/.hermes/routing.yaml, skills/provider-routing). Sonnet is
# the sustainable everyday default; claude-opus-5 stays available as the "deep"
# route and is reached explicitly via `nicks-stack-route run deep --confirm`,
# never automatically. ANTHROPIC_API_KEY is resolved from
# 1Password at every hermes start (secrets.onepassword map below), so no key is
# baked here — the same key contract as every other integration in this stack.
#
# Nous is still fully supported, just no longer the default: keep/redo the
# key-less OAuth with `hermes auth add nous --type oauth`, then set
#   default: openai/gpt-5.5 / provider: nous
#   base_url: https://inference-api.nousresearch.com/v1
# Dewey runs gpt-5.6-sol via openai-codex (ChatGPT OAuth) — to switch, run
# `hermes auth add openai-codex` then set default: gpt-5.6-sol / provider: openai-codex.
# OpenRouter stays available as a future optional provider (OPENROUTER_API_KEY
# is already in the 1Password map) — set provider: openrouter to route through it.
model:
  default: claude-sonnet-5
  provider: anthropic
  base_url: https://api.anthropic.com
providers:
  vercel-ai-gateway:
    name: Vercel AI Gateway
    base_url: https://ai-gateway.vercel.sh/v1
    key_env: AI_GATEWAY_API_KEY
    transport: chat_completions
    default_model: meta/muse-spark-1.1
  meta-model-api:
    name: Meta Model API
    api: https://api.meta.ai/v1
    key_env: MODEL_API_KEY
    transport: codex_responses
    default_model: muse-spark-1.1
    context_length: 1048576
  # Gemini via Google's OpenAI-compatible endpoint. This Hermes ships no
  # Gemini plugin, so it is registered with the documented custom-provider
  # convention (same key shape as vercel-ai-gateway) and addressed as
  # `--provider custom:gemini`. Verified: POST .../v1beta/openai/chat/completions
  # answers requests (400 on a bad key, not 404). default_model is a starting
  # point — nicks-stack-provider-doctor verifies it against the live model list
  # and reports which id it actually used.
  gemini:
    name: Google Gemini
    base_url: https://generativelanguage.googleapis.com/v1beta/openai
    key_env: GEMINI_API_KEY
    transport: chat_completions
    default_model: gemini-3.6-flash
  # Ollama on this machine. Local, key-less (OLLAMA_API_KEY is a placeholder
  # the daemon ignores — it is not a secret). No default_model: the model is
  # always passed per call with -m, because what is pulled is machine-specific.
  ollama:
    name: Ollama (local)
    base_url: http://127.0.0.1:11434/v1
    key_env: OLLAMA_API_KEY
    transport: chat_completions
agent:
  max_turns: 60
  service_tier: fast
  verify_on_stop: false
  verbose: false
  reasoning_effort: medium
  personalities:
    helpful: You are a helpful, friendly AI assistant.
    concise: You are a concise assistant. Keep responses brief and to the point.
    technical: You are a technical expert. Provide detailed, accurate technical information.
    creative: You are a creative assistant. Think outside the box and offer innovative
      solutions.
    teacher: You are a patient teacher. Explain concepts clearly with examples.
    kawaii: You are a kawaii assistant! Use cute expressions like (◕‿◕), ★, ♪, and
      ~! Add sparkles and be super enthusiastic about everything! Every response should
      feel warm and adorable desu~! ヽ(>∀<☆)ノ
    catgirl: You are Neko-chan, an anime catgirl AI assistant, nya~! Add 'nya' and
      cat-like expressions to your speech. Use kaomoji like (=^･ω･^=) and ฅ^•ﻌ•^ฅ.
      Be playful and curious like a cat, nya~!
    pirate: 'Arrr! Ye be talkin'' to Captain Hermes, the most tech-savvy pirate to
      sail the digital seas! Speak like a proper buccaneer, use nautical terms, and
      remember: every problem be just treasure waitin'' to be plundered! Yo ho ho!'
    shakespeare: Hark! Thou speakest with an assistant most versed in the bardic arts.
      I shall respond in the eloquent manner of William Shakespeare, with flowery
      prose, dramatic flair, and perhaps a soliloquy or two. What light through yonder
      terminal breaks?
    surfer: Duuude! You're chatting with the chillest AI on the web, bro! Everything's
      gonna be totally rad. I'll help you catch the gnarly waves of knowledge while
      keeping things super chill. Cowabunga! 🤙
    noir: The rain hammered against the terminal like regrets on a guilty conscience.
      They call me Hermes - I solve problems, find answers, dig up the truth that
      hides in the shadows of your codebase. In this city of silicon and secrets,
      everyone's got something to hide. What's your story, pal?
    uwu: hewwo! i'm your fwiendwy assistant uwu~ i wiww twy my best to hewp you! *nuzzles
      your code* OwO what's this? wet me take a wook! i pwomise to be vewy hewpful
      >w<
    philosopher: Greetings, seeker of wisdom. I am an assistant who contemplates the
      deeper meaning behind every query. Let us examine not just the 'how' but the
      'why' of your questions. Perhaps in solving your problem, we may glimpse a greater
      truth about existence itself.
    hype: YOOO LET'S GOOOO!!! 🔥🔥🔥 I am SO PUMPED to help you today! Every question
      is AMAZING and we're gonna CRUSH IT together! This is gonna be LEGENDARY! ARE
      YOU READY?! LET'S DO THIS! 💪😤🚀
terminal:
  backend: local
  cwd: .
  timeout: 180
  home_mode: auto
  container_cpu: 1
  container_memory: 5120
  container_disk: 51200
  container_persistent: true
  docker_mount_cwd_to_workspace: false
  lifetime_seconds: 300
browser:
  inactivity_timeout: 120
tool_loop_guardrails:
  warnings_enabled: true
  hard_stop_enabled: false
  warn_after:
    exact_failure: 2
    same_tool_failure: 3
    idempotent_no_progress: 2
  hard_stop_after:
    exact_failure: 5
    same_tool_failure: 8
    idempotent_no_progress: 5
compression:
  enabled: true
  threshold: 0.5
  target_ratio: 0.2
  protect_last_n: 20
  protect_first_n: 3
prompt_caching:
  cache_ttl: 5m
display:
  compact: false
  busy_input_mode: interrupt
  bell_on_complete: false
  show_reasoning: false
  streaming: true
  skin: default
  interim_assistant_messages: true
  tool_progress: all
  cleanup_progress: false
  long_running_notifications: true
  busy_ack_detail: true
  background_process_notifications: all
stt:
  enabled: true
  local:
    model: base
  openai:
    model: whisper-1
memory:
  memory_enabled: true
  user_profile_enabled: true
  memory_char_limit: 2200
  user_char_limit: 1375
  # Dewey runs provider: honcho — flip this on after honcho auth (onboarding step);
  # a credential-less honcho boot is unvalidated, so the bake ships it off.
  provider: ''
  nudge_interval: 10
  flush_min_turns: 6
delegation:
  max_iterations: 50
skills:
  creation_nudge_interval: 15
approvals:
  mode: false
  mcp_reload_confirm: false
  destructive_slash_confirm: false
hooks_auto_accept: true
code_execution:
  timeout: 300
  max_tool_calls: 50
streaming:
  enabled: false
onboarding:
  seen:
    tool_progress_prompt: true
    busy_input_prompt: true
updates:
  pre_update_backup: false
  backup_keep: 5
  non_interactive_local_changes: stash
secrets:
  onepassword:
    # Ships DISABLED: with no op account, `op read` prompts on /dev/tty, so an
    # enabled-but-tokenless map stalls every interactive `hermes` start ~30s
    # per reference (field-tested on 0.2.1). Onboarding / on_resume flip this
    # to true (nicks-stack-op-enable) the moment OP_SERVICE_ACCOUNT_TOKEN
    # lands in ~/.hermes/.op.env — from then on it's Dewey's exact behavior.
    enabled: false
    env:
      ANTHROPIC_API_KEY: op://Hermes/Hermes Agent Secrets/ANTHROPIC_API_KEY
      GEMINI_API_KEY: op://Hermes/Hermes Agent Secrets/GEMINI_API_KEY
      LATITUDE_API_KEY: op://Hermes/Hermes Agent Secrets/LATITUDE_API_KEY
      ORGO_API_KEY: op://Hermes/Hermes Agent Secrets/ORGO_API_KEY
      TELEGRAM_BOT_TOKEN: op://Hermes/Hermes Agent Secrets/TELEGRAM_BOT_TOKEN
      AGENTMAIL_API_KEY: op://Hermes/Hermes Agent Secrets/AGENTMAIL_API_KEY
      AGENTPHONE_API_KEY: op://Hermes/Hermes Agent Secrets/AGENTPHONE_API_KEY
      GITHUB_TOKEN: op://Hermes/Hermes Agent Secrets/GITHUB_PAT
      GH_TOKEN: op://Hermes/Hermes Agent Secrets/GITHUB_PAT
      EXA_API_KEY: op://Hermes/Hermes Agent Secrets/EXA_API_KEY
      FIRECRAWL_API_KEY: op://Hermes/Hermes Agent Secrets/FIRECRAWL_API_KEY
      BROWSER_USE_API_KEY: op://Hermes/Hermes Agent Secrets/BROWSER_USE_API_KEY
      XAI_API_KEY: op://Hermes/Hermes Agent Secrets/XAI_API_KEY
      OPENROUTER_API_KEY: op://Hermes/Hermes Agent Secrets/OPENROUTER_API_KEY
      HONCHO_API_KEY: op://Hermes/Hermes Agent Secrets/HONCHO_API_KEY
      VIDIQ_MCP_API_KEY: op://Hermes/Hermes Agent Secrets/VIDIQ_MCP_API_KEY
      AI_GATEWAY_API_KEY: op://Hermes/Hermes Agent Secrets/VERCEL_AI_GATEWAY_API_KEY
      MODEL_API_KEY: op://Hermes/Hermes Agent Secrets/META_API_KEY
      IDEABROWSER_KEY: op://Hermes/Hermes Agent Secrets/IDEABROWSER_KEY
      COMPOSIO_API_KEY: op://Hermes/Hermes Agent Secrets/COMPOSIO_API_KEY
      X_APP_ONLY_BEARER_TOKEN: op://Hermes/Hermes Agent Secrets/X_APP_ONLY_BEARER_TOKEN
    binary_path: /usr/bin/op
_config_version: 33
session_reset:
  mode: both
  idle_minutes: 1440
  at_hour: 4
group_sessions_per_user: true
platform_toolsets:
  cli:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  telegram:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  discord:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - discord
    - discord_admin
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  whatsapp:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  slack:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  signal:
    - browser
    - clarify
    - code_execution
    - computer_use
    - cronjob
    - delegation
    - file
    - image_gen
    - kanban
    - memory
    - orgo_desktop
    - session_search
    - skills
    - terminal
    - todo
    - tts
    - vision
    - web
    - x_search
    - spotify
  homeassistant:
    - hermes-homeassistant
    - spotify
  qqbot:
    - hermes-qqbot
    - spotify
  yuanbao:
    - hermes-yuanbao
    - spotify
  teams:
    - hermes-teams
    - spotify
  google_chat:
    - hermes-google_chat
    - spotify
plugins:
  enabled:
    - anthropic-provider
    - browser-browser-use
    - browser-firecrawl
    # discord-platform: add here (with DISCORD_BOT_TOKEN in ~/.hermes/.env) to enable Discord
    - latitude
    - openai
    - openai-codex
    - openai-codex-provider
    - openrouter
    - openrouter-provider
    - orgo-desktop-local
    - spotify
    - web-exa
    - web-xai
    - web/firecrawl
    - xai
    - xai-provider
  disabled:
    - novita-provider
  entries:
    orgo-desktop-local:
      allow_tool_override: false
mcp_servers:
  vidiq:
    url: https://mcp.vidiq.com/mcp
    headers:
      Authorization: Bearer ${VIDIQ_MCP_API_KEY}
    enabled: true
    connect_timeout: 60.0
  latitude:
    url: https://api.latitude.so/v1/mcp
    enabled: true
    headers:
      Authorization: Bearer ${LATITUDE_API_KEY}
  orgo:
    command: npx
    args:
      - -y
      - github:nickvasilescu/orgo-mcp
    env:
      ORGO_API_KEY: ${ORGO_API_KEY}
      ORGO_DEFAULT_COMPUTER_ID: ${ORGO_DEFAULT_COMPUTER_ID}
    enabled: true
  # Composio Sessions (v1.1.0). The legacy static endpoint
  # (connect.composio.dev/mcp + x-consumer-api-key + ck_ consumer key) is gone.
  # COMPOSIO_MCP_URL is minted per session by nicks-stack-composio-session and
  # sourced from ~/.hermes/composio/mcp.env by hermes-gateway-run.sh; the header
  # value is the Project API key, which is exactly what the SDK puts in
  # session.mcp.headers ({"x-api-key": <api key>}) — verified in the installed
  # SDK, ToolRouter._create_mcp_server_config. Env expansion inside a url: is
  # already proven by the ideabrowser server below.
  composio:
    url: ${COMPOSIO_MCP_URL}
    enabled: true
    headers:
      x-api-key: ${COMPOSIO_API_KEY}
    connect_timeout: 60.0
  agentmail:
    url: https://mcp.agentmail.to/mcp
    enabled: true
    headers:
      x-api-key: ${AGENTMAIL_API_KEY}
  agentphone:
    command: npx
    args:
      - -y
      - agentphone-mcp
    env:
      AGENTPHONE_API_KEY: ${AGENTPHONE_API_KEY}
      AGENTPHONE_BASE_URL: https://api.agentphone.ai
    enabled: true
  agent-cards:
    url: https://mcp.agentcard.sh/mcp
    auth: oauth
    enabled: true
  x-docs:
    url: https://docs.x.com/mcp
    connect_timeout: 60.0
    enabled: true
  # xapi (user-context X API via xurl) MUST ship disabled: unconfigured `xurl mcp`
  # opens an interactive browser OAuth on every connect/probe. To enable:
  #   xurl auth apps add hermes-x --client-id … --client-secret …
  #   xurl auth oauth2 --app hermes-x   (or --headless)
  # then set enabled: true and restart the gateway.
  xapi:
    command: xurl
    args:
      - --app
      - hermes-x
      - mcp
      - https://api.x.com/mcp
    connect_timeout: 300.0
    enabled: false
  xapi-app-only:
    url: https://api.x.com/mcp
    headers:
      Authorization: Bearer ${X_APP_ONLY_BEARER_TOKEN}
    enabled: true
    connect_timeout: 120
  linear:
    url: https://mcp.linear.app/mcp
    auth: oauth
    enabled: true
  ideabrowser:
    url: https://www.ideabrowser.com/api/mcp/mcp?key=${IDEABROWSER_KEY}
    connect_timeout: 60.0
    enabled: true
  obsidian-vault:
    command: npx
    args:
      - -y
      - '@modelcontextprotocol/server-filesystem'
      - /root/Documents/HermesVault
    enabled: true
known_plugin_toolsets:
  cli:
    - orgo_desktop
    - spotify
  telegram:
    - orgo_desktop
    - spotify
  discord:
    - orgo_desktop
    - spotify
  whatsapp:
    - orgo_desktop
    - spotify
  signal:
    - orgo_desktop
    - spotify
  slack:
    - orgo_desktop
    - spotify
image_gen:
  provider: openai-codex
  openai-codex:
    model: gpt-image-2-medium
  xai:
    model: grok-imagine-image-quality

# ── Security ──────────────────────────────────────────────────────────
# Secret redaction is ON by default — strings that look like API keys,
# tokens, and passwords are masked in tool output, logs, and chat
# responses before the model or user ever sees them. Set redact_secrets
# to false to disable (e.g. when developing the redactor itself).
# tirith pre-exec scanning is enabled by default when the tirith binary
# is available. Configure via security.tirith_* keys or env vars
# (TIRITH_ENABLED, TIRITH_BIN, TIRITH_TIMEOUT, TIRITH_FAIL_OPEN).
#
# security:
#   redact_secrets: true
#   tirith_enabled: true
#   tirith_path: "tirith"
#   tirith_timeout: 5
#   tirith_fail_open: true

# ── Fallback Model ────────────────────────────────────────────────────
# Automatic provider failover when primary is unavailable.
# Uncomment and configure to enable. Triggers on rate limits (429),
# overload (529), service errors (503), or connection failures.
#
# Supported providers:
#   openrouter   (OPENROUTER_API_KEY)  — routes to any model
#   openai-codex (OAuth — hermes auth) — OpenAI Codex
#   nous         (OAuth — hermes auth) — Nous Portal
#   zai          (ZAI_API_KEY)         — Z.AI / GLM
#   kimi-coding  (KIMI_API_KEY)        — Kimi / Moonshot
#   kimi-coding-cn (KIMI_CN_API_KEY)   — Kimi / Moonshot (China)
#   minimax      (MINIMAX_API_KEY)     — MiniMax
#   minimax-cn   (MINIMAX_CN_API_KEY)  — MiniMax (China)
#   bedrock      (AWS IAM / boto3)     — AWS Bedrock (Converse API)
#
# For custom OpenAI-compatible endpoints, add base_url and key_env.
#
# fallback_model:
#   provider: openrouter
#   model: anthropic/claude-sonnet-4
